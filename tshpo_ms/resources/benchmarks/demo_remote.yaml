---
- name: __defaults__ # dataset name
  folds: 3  # kfold to verify
  max_runtime_seconds: 30
  description: "multiclass test dataset"
  output_dir: "./outputs/demo_test"
  model_name: "autosklearn"
  metric: "auc"
  seed: 42
  dataset_home: "/remote-home/cs_acmis_xxx/automl/deps/datasets/datasets"

- name: credit-g # dataset name
- name: kc1 # dataset name

